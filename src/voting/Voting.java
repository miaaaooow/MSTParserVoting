package voting;

public class Voting {
	
	/**
	 * 
	 * @param labeled - if labeled or unlabeled mode
	 * @param parsers - the number of parsers we read for each tree to make voting
	 */
	public void readGraphs(boolean labeled, int parsers) {
		
	}
}
